spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bdf
    username: bdf_user
    password: bdf_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: ${JWT_SECRET:}
  expirationMs: 86400000
